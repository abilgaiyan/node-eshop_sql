<%- include('../includes/head.ejs') %>
   <link rel="stylesheet" href="/css/cart.css">
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>
        <main>
            <% if (orders.length <= 0) { %>
                <h1>No Orders!</h1>
            <% } else { %>
                <div class="centered">
                  <h1>Your Orders</h1>
                </div>
                <ul>
                    <% orders.forEach(order => { %>
                        <li>
                            <div class="centered">
                              <h1># <%= order.id %></h1>
                            </div>
                            
                            <ul class="cart__item-list">
                                <% order.products.forEach(product => { %>
                                    <li class="cart__item">
                                     <h1><%= product.title %></h1>
                                     <h2>Quantity: <%= product.orderItem.quantity %></h2>
                                     <h2>Price: <%= product.orderItem.price %></h2>
                                   </li>
        
                                <% }); %>
                            </ul>
                        </li>
                    <% }); %>
                </ul>
            <% } %>
        </main>
        <%- include('../includes/end.ejs') %>